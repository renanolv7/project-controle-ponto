import 'package:flutter/material.dart';

class BtnBatidaPonto extends StatelessWidget {
  const BtnBatidaPonto({super.key});

  @override
  Widget build(BuildContext context) {

    return Scaffold(
      body: Container(
        color: Colors.white,
        width: double.infinity,

        child: SizedBox(
          height: 50,
          width: 50,
        ),

      ),
    );
  }
}